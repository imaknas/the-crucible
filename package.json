{
  "name": "the-crucible",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "description": "Multi-model reasoning arena and deliberation graph executor.",
  "scripts": {
    "release": "release-it",
    "test:e2e": "npm run e2e:seed && playwright test",
    "test:e2e:ui": "npm run e2e:seed && playwright test --ui",
    "e2e:seed": "uv run --project backend python e2e/seed_db.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@release-it/conventional-changelog": "^10.0.6",
    "release-it": "^18.1.2"
  }
}
